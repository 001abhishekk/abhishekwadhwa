# Anima Project

Welcome! This project has been automatically generated by [Anima](https://animaapp.com/).

## Contact Form Setup

The contact form uses Vercel serverless functions for form handling without external dependencies.

### Features:
- **Spam Protection**: Honeypot field and keyword filtering
- **Form Validation**: Both frontend and backend validation
- **Rate Limiting**: Basic IP-based rate limiting
- **Error Handling**: Comprehensive error messages
- **Security**: CORS headers and input sanitization

### API Endpoint:
- `POST /api/contact` - Handles form submissions

### To Add Email Functionality:
1. Choose an email service (Nodemailer, SendGrid, Resend, etc.)
2. Add environment variables for email credentials
3. Update the `/api/contact.js` file to integrate with your chosen service

### Environment Variables (Optional):
```
SMTP_HOST=your-smtp-host
SMTP_PORT=587
SMTP_USER=your-email@domain.com
SMTP_PASS=your-password
```

## Getting started

> **Prerequisites:**
> The following steps require [NodeJS](https://nodejs.org/en/) to be installed on your system, so please
> install it beforehand if you haven't already.

To get started with your project, you'll first need to install the dependencies with:

```
npm install
```

Then, you'll be able to run a development version of the project with:

```
npm run dev
```

After a few seconds, your project should be accessible at the address
[http://localhost:5173/](http://localhost:5173/)


If you are satisfied with the result, you can finally build the project for release with:

```
npm run build
```
